#include "main.h"

/**
 * _memset - print memory
 * @s: parameter 1
 * @n: parameter 2
 * @b: parameter
 * Return: always 0
 */

char *_memset(char *s, char b, unsigned int n)
{
	int i;

	for (i = 0; n > 0; i++)
	{
		s[i] = b;
		n--;
	}
	return (s);
}
